<VirtualHost *:80> #define que se va a usar el puerto 80, osea va a tomar todo lo que se mande ahi
    DocumentRoot /var/www/html/public #le dice a apache que la carpeta raiz es esa (la que se ve publica)
    <Directory /var/www/html/public> #1este bloque que se llama directory le da las reglas de esa carpeta
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>